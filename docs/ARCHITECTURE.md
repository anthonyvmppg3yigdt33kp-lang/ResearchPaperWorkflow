# Architecture

The maintained V4.3 architecture reference is the root document:

`ARCHITECTURE.md`

Use that file for the current four-layer control model, AI harness design,
agent-cluster collaboration, 20-stage truth-layer pipeline, loop state machine,
human-in-the-loop checkpoint policy, artifact ledger design, and quality-gate
information chain.

This compatibility document is kept so historical links to
`docs/ARCHITECTURE.md` still resolve.
